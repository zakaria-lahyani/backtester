name: "{{ signal_name }}_{{ period }}_{{ timeframe }}_{{ oversold_value }}_{{ overbought_value }}"
timeframes: ["{{ timeframe }}"]

entry:
  long:
    mode: "all"
    conditions:
      - signal: "{{ signal_name }}_{{ period }}_percent_k"
        operator: "crosses_below"
        value: "{{ oversold_value }}"
        timeframe: "{{ timeframe }}"

exit:
  long:
    mode: "any"
    conditions:
      - signal: "{{ signal_name }}_{{ period }}_percent_k"
        operator: "crosses_above"
        value: "{{ overbought_value }}"
        timeframe: "{{ timeframe }}"

risk:
  position_sizing:
    type: "fixed"
    value: 1
    atr_distance: {{ atr_distance | float }}

  sl:
    type: "fixed"
    value: 10

  tp:
    type: "fixed"
    value: 1100.0
